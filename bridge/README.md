# Reconstruction communications bridge

Reserved for the external communications layer:

Inform 7 -> Vorple -> HTTP/WebSocket gateway -> Eggdrop/Tcl -> IRC.

The bridge is deliberately outside the Inform VM. No credentials belong in the story file or browser bundle.

See docs/IRC_BRIDGE.md.
